pre-commit:
  jobs:
    - name: guardrails
      run: pnpm exec node .lefthook/pre-commit/run.mjs

pre-push:
  jobs:
    - name: guardrails
      run: pnpm exec node .lefthook/pre-push/run.mjs

commit-msg:
  jobs:
    - name: commitlint
      run: pnpm exec commitlint --edit {1}
