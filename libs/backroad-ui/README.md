# backroad-ui

The Backroad design-system primitive layer: shadcn/ui components and Vercel AI
Elements. Pure presentational primitives with no dependency on the socket
runtime or chart registry — `backroad-components` (the renderer layer), the
example app, and the Ladle/Storybook stories all consume primitives from here.
